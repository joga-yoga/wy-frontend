export type Language = {
  language_name: string;
  language_code: string;
};

export type Price = {
  min_price: number;
  max_price: number;
};

export type PeriodSet = {
  start_date: string;
  end_date: string;
  name: string;
};
