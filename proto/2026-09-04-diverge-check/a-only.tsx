// reframe: brand identity held, component conventions free — so no product component imports.
export default function Only() {
  return (
    <div style={{ padding: 24, fontFamily: "var(--font-hind-siliguri)" }}>
      <p style={{ fontSize: 22, fontWeight: 600 }}>Wariant rozbieżny</p>
      <p style={{ marginTop: 8, color: "#666" }}>Bez zapisanego wyniku — indeks ma to pokazać.</p>
    </div>
  );
}
